# Copy this file to ".env.local" and paste in your own values from
# Supabase -> Project Settings -> API. Restart `npm run dev` after editing.
#
# Until these are filled in, the app runs in local "demo mode".
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
